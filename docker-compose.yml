name: monetra

services:
  monetra-server:
    build: .
    image: monetra-server:latest
    container_name: monetra-server
    ports:
      - "127.0.0.1:9000:8000"
      - "127.0.0.1:9090:9090"
    env_file:
      - .env
    restart: unless-stopped
